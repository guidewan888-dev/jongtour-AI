const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({ datasources: { db: { url: 'postgresql://postgres.qterfftaebnoawnzkfgu:zX70oOfCtmYFTimy@aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true' } } }); async function test() { try { const tours = await prisma.tour.findMany({ take: 1 }); console.log('SUCCESS:', tours.length); } catch(e) { console.error('FAILED:', e); } finally { await prisma.$disconnect(); } } test();
